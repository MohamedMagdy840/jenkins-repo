pipeline {
    agent any

    stages {
        stage('Print Hello') {
            steps {
                echo 'Hello'
            }
        }

        stage('Print Jenkins') {
            steps {
                echo 'Jenkins'
            }
        }
    }
}
